require "spec_helper"

describe DevcampViewToolAvery do
  it "has a version number" do
    expect(DevcampViewToolAvery::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
